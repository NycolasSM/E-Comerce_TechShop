{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "products": "json-server --watch ./db/products.json --port 3001",
    "users": "json-server --watch ./db/users.json --port 3002"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
